import './banner.css';

const Banner = () => {
    return (
        <header className="header">
            <img src="/images/banner.png" alt="Organo logo" />
        </header>
    );
};

export default Banner;
